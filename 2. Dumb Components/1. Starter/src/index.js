import React from 'react'
import ReactDOM from 'react-dom'
import Hello from './Hello'

// Complete the ReactDom.render method with the right first parameter parameter
// The objective is to load the Hello component in the html root tag
ReactDOM.render(
	, // What
	document.getElementById('root')
);